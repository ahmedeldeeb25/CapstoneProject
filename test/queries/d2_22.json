{
    "title": "Select Title, Average from Courses where ID is 400 group by Title",
    "query": "In courses dataset courses grouped by Title, find entries whose Department is \"cpsc\"; show Department and avgGrade, where avgGrade is the SUM of Average.",
    "response": {
        "code": 400,
        "body": {
            "error": "All COLUMNS keys need to be either in GROUP or in APPLY"
        }
    }
}
