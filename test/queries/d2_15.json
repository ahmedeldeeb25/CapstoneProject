{
    "title": "Select Title, Average from Courses where ID is 400 group by Title",
    "query": "In courses dataset courses grouped by Title, find entries whose ID is \"400\"; show UUID and min, where min is the MIN of Average; sort in descending order by min and UUID.",
    "response": {
        "code": 400,
        "body": {
            "error": "All COLUMNS keys need to be either in GROUP or in APPLY"
        }
    }
}
